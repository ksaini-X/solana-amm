1. User created a POOL
    i. User sends token A
    i. User sends token B

In token transfers
    Account<'info, AccpuntTpe> is not used
    WHY?
        - Deserializes into specif accountype
        - InterfaceAccount accepts both TokenProgram and Token2020Program

If a state Pubkey is not set in instruction
like 
account{
    vaultPubkey:Pubkey
}
    in ix if its not setthen it defaults to 111111111



Versioning

if the state of a program is updated, the different data can be fetched by Versioning
put the first parameter as a u8 called Versioning
and data can be filtered using the dataMcp in fetch


I had made the tokenMint acc mut, it started giving problems in CPI